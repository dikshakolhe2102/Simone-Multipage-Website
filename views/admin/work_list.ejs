<%- include('header.ejs'); %>

    <div class="main">
        <header class="topbar">
        <h2>Work table</h2>
        <div class="user">Welcome, Admin</div>
        </header>

        <section class="content">
        <div class="panel">
            <h3>All users</h3>

            <table>
            <thead>
                <tr>
                <th>ID</th>
                <th>Work Image</th>
                <th>Work Title</th>
                <th>Work Description</th>
                <th>Status</th>
                </tr>
            </thead>
            <tbody>
                <% for(var i=0; i<data.length; i++){ %>
                <tr>
                <td><%= i+1 %></td>
                <td>
                    <img src="/image/<%= data[i].w_img %>" height="100px" />
                </td>
                <td><%= data[i].w_title %></td>
                <td><%= data[i].w_desc %></td>
                <td>
                    <a href="/admin/delete/<%= data[i].wid %>/<%= encodeURIComponent(data[i].w_img) %>">
                        <button class="badge badge-ok">Delete</button>
                    </a>
                    <span class="badge badge-ok">Edit</span>
                </td>
                </tr>
                <% } %>
            </tbody>
            </table>
        </div>
        </section>
    </div>
    </div>
<%- include('footer.ejs'); %>
